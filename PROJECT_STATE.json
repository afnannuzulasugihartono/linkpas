{
  "project": "LINKPAS",
  "milestone": "APK_BETA",
  "status": "BLOCKED",
  "current_checkpoint": "A3",
  "last_completed_checkpoint": "A2",
  "completed_checkpoints": ["A1", "A2"],
  "next_action": "Recheck GitHub Actions run 34862894617 for PR #3. If successful, verify the debug APK artifact, record the output path, complete A3, and advance to A4. If failed, inspect the failed job logs and fix only the A3 build blocker.",
  "blocker": "Required A3 GitHub Actions build run 34862894617 is queued; repository rules require stopping at the CI gate until it completes.",
  "in_progress_checkpoint_evidence": {
    "checkpoint": "A3",
    "result": "WAITING_FOR_CI",
    "build_command": "gradle --no-daemon :app:assembleDebug",
    "expected_apk_path": "android/app/build/outputs/apk/debug/app-debug.apk",
    "workflow": ".github/workflows/android-debug.yml",
    "workflow_run_id": 34862894617,
    "workflow_name": "Android Debug APK",
    "workflow_status": "queued",
    "validation_pr": 3,
    "validation_pr_head": "876cf903b7f0d7d2e3f97aea722793ab74e75f67"
  },
  "continuation_semantics": {
    "trigger": "lanjutkan",
    "checkpoints_per_turn": 1,
    "target_timebox_minutes": 15,
    "hard_timebox_minutes": 18,
    "must_finish_under_minutes": 20,
    "must_split_before_execution_if_too_large": true,
    "must_update_state_after_checkpoint": true,
    "must_stop_before_next_checkpoint": true
  }
}
