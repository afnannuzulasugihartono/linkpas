name: Android D4b Runtime Diagnostics

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/android-d4b-runtime.yml'
      - '.github/scripts/d4b-runtime.sh'
      - '.github/scripts/d4b-http-relay.py'
      - 'android/app/build.gradle'
      - 'android/app/src/debug/AndroidManifest.xml'
      - 'android/app/src/main/java/id/barangpas/linkpas/NativeDiagnostics.java'
      - 'android/app/src/main/java/id/barangpas/linkpas/NativeDiagnosticTransport.java'

permissions:
  contents: read

jobs:
  validate-native-diagnostics:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.11.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Build D4b debug APK with localhost relay endpoint
        working-directory: android
        run: gradle --no-daemon :app:assembleDebug -PlinkpasDiagnosticsEndpoint=http://127.0.0.1:8765/

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Start validation-only Supabase relay
        shell: bash
        run: |
          nohup python3 .github/scripts/d4b-http-relay.py >/tmp/d4b-http-relay.log 2>&1 &
          sleep 1
          python3 - <<'PY'
          import socket
          s = socket.create_connection(('127.0.0.1', 8765), timeout=2)
          s.close()
          print('D4B_HTTP_RELAY=ready')
          PY

      - name: Run controlled native diagnostic on Android emulator
        id: runtime
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          target: google_apis
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          emulator-options: -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim
          script: bash .github/scripts/d4b-runtime.sh

      - name: Upload D4b report ID evidence
        uses: actions/upload-artifact@v4
        with:
          name: linkpas-d4b-report-id
          path: d4b-report-id.txt
          if-no-files-found: error
          retention-days: 7
